# Laema progress-UID checkpoint — 2026-09-11

This schema-3 checkpoint records the portable progress-UID workflow and the complete current shared checkout after the presentation-tooling pass.

## Current source scope

- Base revision before this save: `cd31222`, with `main` aligned to `origin/main`.
- Workflow changes: `AGENTS.md`, `docs/README.md`, `tools/README.md`, and `tools/checkpoint.ps1` now carry and compare portable `progress_uid` values; matching source/local UIDs are emitted as `skipped_dormant` during load.
- Presentation scope: the prior Heat-loop deck is modified and marked deleted in one path, a renamed manual-backup copy, a trim/static deck, an HTML preview, a Slide 16 PNG/import file, and a keyframe MP4 are present as current working-tree files.

## Task sweep

All ten currently listed, unarchived Laema tasks were queried. PM, Art, and Marketing have newer material completed rounds; the other tasks retain their previous completed rounds. Every task has a compact note and a portable progress UID in the manifest.

## Evidence boundary

UID equality is a recovery optimization, not gameplay validation. Presentation files and reported PowerPoint checks do not establish Godot gameplay, build, compiler, or automated-test evidence.
